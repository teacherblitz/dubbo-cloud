server:
  port: 2000

spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      discovery:
        server-addr: @nacos.server@
        namespace: f584899c-276b-4385-b6c8-1f0f3699c53c
      config:
        server-addr: @nacos.server@
        namespace: f584899c-276b-4385-b6c8-1f0f3699c53c
        file-extension: yml
        shared-configs:
          - ${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
  profiles:
    active: @profiles.active@
